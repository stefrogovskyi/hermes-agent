User: Stefan Rogovskiy (COO Navo). Archie Wright is the Content Strategist & Chief Copywriter (@archiewrightbot).
§
Ранее были проблемы с `execute_code` для больших файлов, `terminal` без `python3`, форматом `cronjob` и MIME-типами при загрузке XLSX в Google Таблицы. Нужен строгий контроль синтаксиса и путей.
§
Stefan ценит высококачественный человеческий не-AI контент и жёстко проверяет рерайты на скрытый копипаст (ловил дословные куски даже после самопроверки агента) — при рерайте статей всегда закладывать независимый аудиторский проход + собственную программную сверку n-грамм с оригиналом, не верить только самоотчёту субагента. Для контент-генерации требует именно Claude Pro/Max через OAuth (CLAUDE_CODE_OAUTH_TOKEN), НЕ API-ключ. Предпочитает нативные Google Sheets, не TSV/XLSX round-trip; таблицы имеют кастомные (не 'Sheet1') названия вкладок и заголовок в строке 1, который нельзя затирать. Прежде чем заявлять об успехе, ожидает реальную проверку фактического содержимого (не просто exit code/updatedCells) — уже ловил скопированный текст, отсутствие meta title/description, нерерайтнутый заголовок, затёртые заголовки таблицы. Низкая терпимость к повторяющимся ошибкам и циклам извинений — чинить корневую причину сразу, не повторять попытки вслепую.